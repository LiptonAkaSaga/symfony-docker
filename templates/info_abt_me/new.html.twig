{% extends 'base.html.twig' %}

{% block title %}Dodaj nową informację{% endblock %}

{% block body %}
    <h1>Dodaj nową informację</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Zapisz</button>
    {{ form_end(form) }}

    <a href="{{ path('app_about_me') }}" class="btn btn-secondary mt-3">Powrót</a>
{% endblock %}
