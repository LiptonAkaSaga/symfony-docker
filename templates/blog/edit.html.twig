{% extends 'base.html.twig' %}

{% block title %}Edytuj artykuł{% endblock %}

{% block body %}
    <h1>Edytuj artykuł</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Zapisz zmiany</button>
    {{ form_end(form) }}
    <a href="{{ path('article_list') }}" class="btn btn-secondary">Anuluj</a>
{% endblock %}
